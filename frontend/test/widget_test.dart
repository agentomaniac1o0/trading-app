import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('TradingApp renders without error', (WidgetTester tester) async {
    // TODO: Add integration tests once backend API is available
    expect(1 + 1, 2);
  });
}